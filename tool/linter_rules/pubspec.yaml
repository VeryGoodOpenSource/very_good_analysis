name: linter_rules
description: Tools that help maintain Very Good Analysis rules.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.6.0
  bump_version:
    path: ../bump_version
  collection: ^1.19.1
  http: ^1.2.1
  meta: ^1.16.0
  path: ^1.9.0
  yaml: ^3.1.2
  yaml_edit: ^2.2.2

dev_dependencies:
  test: ^1.25.14
  very_good_analysis:
    path: ../../
